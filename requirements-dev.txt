# Development + research dependencies (tests, lint, backtesting/TA).
# Install on top of runtime:  pip install -r requirements.txt -r requirements-dev.txt

# --- Tests / lint / types ---
pytest>=8.0
pytest-asyncio>=0.23
ruff>=0.6
mypy>=1.11

# --- Research / backtesting / TA (heavier; optional for running the app) ---
pandas-ta>=0.3.14b0
statsmodels>=0.14
vectorbt>=0.26
